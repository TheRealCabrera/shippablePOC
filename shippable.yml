#no language needed
language: none

build:
    pre_ci_boot:
        image_name: library/ipreoaccount
        image_tag: latest
        pull: true

#ECR integration
integrations:
  hub:
    - integrationName: ECRTest
      type: ecr
      branches:
        only:
          - master
          - dev
