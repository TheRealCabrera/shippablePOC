#resources yml file

resources:

#image being used for testing
  - name: testImage
    type: image
    integration: ECRTest
    pointer:
      sourceName: "library/ipreoaccount"
    seed:
      versionName: "latest"
      
#cluster being used for deployment
  - name: aws-test
    type: cluster
    integration: aws-test
    pointer:
      sourceName : "default"
      region: "us-west-2"

